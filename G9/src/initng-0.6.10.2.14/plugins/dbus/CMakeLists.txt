INCLUDE_DIRECTORIES( ${CMAKE_SOURCE_DIR}/src ${CMAKE_BINARY_DIR} )
ADD_DEFINITIONS(${GIO_INCLUDE_DIR} ${GLIB_INCLUDE_DIR} ${GIO_CFLAGS} ${GLIB_CFLAGS})
ADD_LIBRARY(dbus MODULE initng_dbus.c)
TARGET_LINK_LIBRARIES(dbus initng ${GIO_LINK_DIR} ${GIO_LINK_FLAGS} ${GLIB_LINK_DIR} ${GLIB_LINK_FLAGS})
INSTALL_TARGETS(${LIB_INSTALL_DIR}/initng dbus)
