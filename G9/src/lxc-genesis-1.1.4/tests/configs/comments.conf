# comment line starting at column 0

  # comment line not starting at column 0

user = username    # comment following a declaration
group=groupname# # the first # is part of the value
# umask=711 this line should be ignored
